
{% extends '::layout.html.twig' %}

{% block body -%}
    <div class="panel-body">
        <div class="table-responsive">
            <table id="dataTables" class="table table-striped table-bordered" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th>date réception </th>
                        <th>body</th>
                        <th>Link</th>
                        <th>Id Message</th>
                        <th>Id Commentaire</th>
                        <th>users</th>
                        <th>nbLikes</th>
                        <th>groupe</th>

                    </tr>
                </thead>
                <tbody>
                    {% for commentaire in commentaires %}
                        <tr class="{{ cycle(['odd', 'even'], loop.index0) }} gradeX">
                            <td><span class="hide">{{commentaire.dateReception|date('Y-m-d H:i:s')}} - </span>{{ commentaire.dateReception|date('d/m/y ') }}</td>
                            <td>{{ commentaire.body }}</td>
                            <td>{{commentaire.link }}</td>
                            <td>{{commentaire.idMessages}}</td>
                            <td>{{commentaire.idCommentaire}}</td>
                            <td>{{commentaire.Users.name }}</td>
                            <td>{{ commentaire.nbLikes}}</td>
                            <td>{{commentaire.Groupe.name}}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>

        {#        {{dump(test)}}#}

    </div>
{% endblock %}

